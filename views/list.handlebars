<h1 class="logo">Lista de <span>Eventos:</span> </h1>

{{#each pontos}}
    <div id="list">
        <h1>{{nome}}</h1>
        <p>Valor: {{valor}}</p>
        <p>Organização: {{org}}</p>
        <p>Data: {{data}}</p>
        <a href="">Onde é isso?</a>
        
   </div>
{{/each}}