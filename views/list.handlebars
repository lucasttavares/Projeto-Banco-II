<h1 class="logo" id="titulolista">Lista de <span>Eventos:</span> </h1>

{{#each pontos}}
    <div id="list">
        <h1>{{nome}}</h1>
        <p>Valor: {{valor}}</p>
        <p>Organização: {{org}}</p>
        <p>Data: {{data}}</p>
        <p>Insira as seguintes coordenadas no campo de pesquisa do google maps:</p>
        <p>{{geometry.coordinates}}</p>
        
   </div>
{{/each}}